Iniciar programa
En una terminal empezaremos con los siguientes comandos:
    - npm install
    - npm run dev
Abrimos otra terminal y ponemos los siguientes comandos:
    - composer install
    - cd public/
    - php -S localhost:3000